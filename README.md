Small collection of personal containers.

Prefixes:

- `box-`: these are intended for usage with distrobox/toolbx.
- `svc-`: these are intended to run as services, via systemd or by spawning them manually.
